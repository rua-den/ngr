<Window x:Class="Ngr.Launcher.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Ngr.Launcher.App.ViewModels"
        Title="NGR Launcher"
        Width="1180"
        Height="760"
        MinWidth="980"
        MinHeight="640"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">
  <Window.Resources>
    <Style x:Key="NavButtonStyle" TargetType="Button">
      <Setter Property="Foreground" Value="#E5E7EB" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="Padding" Value="18,12" />
      <Setter Property="Margin" Value="10,3" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Cursor" Value="Hand" />
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#243247" />
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
      <Setter Property="Foreground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="14,8" />
      <Setter Property="Margin" Value="4" />
      <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
      <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
      <Setter Property="Padding" Value="12,8" />
      <Setter Property="Margin" Value="4" />
      <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style TargetType="TextBox">
      <Setter Property="Padding" Value="8,6" />
      <Setter Property="Margin" Value="0,4,0,10" />
    </Style>

    <Style TargetType="ComboBox">
      <Setter Property="Padding" Value="6,4" />
      <Setter Property="Margin" Value="0,4,0,10" />
    </Style>

    <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
      <Grid Margin="28">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="10" Padding="18" Margin="0,0,0,16">
          <StackPanel>
            <TextBlock Text="Launch a profile" FontSize="18" FontWeight="SemiBold" />
            <DockPanel Margin="0,12,0,0" LastChildFill="False">
              <ComboBox Width="320"
                        ItemsSource="{Binding Profiles}"
                        SelectedItem="{Binding SelectedProfile}"
                        DisplayMemberPath="Name" />
              <Button Content="Launch" Command="{Binding LaunchCommand}" Style="{StaticResource PrimaryButtonStyle}" />
              <Button Content="Cancel selected run" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButtonStyle}" />
            </DockPanel>
            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
          </StackPanel>
        </Border>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Sessions}"
                 SelectedItem="{Binding SelectedSession}"
                 Background="Transparent"
                 BorderThickness="0"
                 HorizontalContentAlignment="Stretch">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="10" Padding="16" Margin="0,0,0,10">
                <StackPanel>
                  <DockPanel>
                    <TextBlock Text="{Binding ProfileName}" FontSize="16" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding Status}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" DockPanel.Dock="Right" />
                  </DockPanel>
                  <TextBlock Text="{Binding Id}" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,2,0,10" />
                  <ItemsControl ItemsSource="{Binding Steps}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Grid Margin="0,3">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="90" />
                            <ColumnDefinition Width="130" />
                            <ColumnDefinition Width="2*" />
                          </Grid.ColumnDefinitions>
                          <TextBlock Text="{Binding ToolName}" />
                          <TextBlock Grid.Column="1" Text="{Binding DelayBeforeSeconds, StringFormat={}{0}s delay}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                          <TextBlock Grid.Column="2" Text="{Binding Status}" FontWeight="SemiBold" />
                          <TextBlock Grid.Column="3" Text="{Binding Error}" Foreground="#E05252" TextWrapping="Wrap" />
                        </Grid>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </StackPanel>
              </Border>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:ToolLibraryViewModel}">
      <Grid Margin="28">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="285" />
          <ColumnDefinition Width="16" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="10" Padding="14">
          <DockPanel>
            <StackPanel DockPanel.Dock="Top">
              <TextBlock Text="Tools" FontSize="18" FontWeight="SemiBold" />
              <StackPanel Orientation="Horizontal" Margin="0,8,0,8">
                <Button Content="New" Command="{Binding NewCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource SecondaryButtonStyle}" />
              </StackPanel>
              <TextBlock Text="Template" FontWeight="SemiBold" />
              <ComboBox ItemsSource="{Binding Templates}" SelectedItem="{Binding SelectedTemplate}" DisplayMemberPath="Name" />
              <Button Content="Apply template" Command="{Binding ApplyTemplateCommand}" Style="{StaticResource SecondaryButtonStyle}" />
            </StackPanel>
            <ListBox ItemsSource="{Binding Tools}"
                     SelectedItem="{Binding SelectedTool}"
                     DisplayMemberPath="Name"
                     BorderThickness="0"
                     Margin="0,12,0,0" />
          </DockPanel>
        </Border>

        <Border Grid.Column="2" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="10" Padding="20">
          <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
              <TextBlock Text="Tool editor" FontSize="18" FontWeight="SemiBold" />
              <Grid Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="16" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                  <TextBlock Text="ID" FontWeight="SemiBold" />
                  <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" />
                  <TextBlock Text="Name" FontWeight="SemiBold" />
                  <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                  <TextBlock Text="Kind" FontWeight="SemiBold" />
                  <ComboBox ItemsSource="{Binding ToolKinds}" SelectedItem="{Binding Kind}" />
                  <TextBlock Text="Target (applications)" FontWeight="SemiBold" />
                  <TextBox Text="{Binding Target, UpdateSourceTrigger=PropertyChanged}" />
                  <TextBlock Text="Arguments" FontWeight="SemiBold" />
                  <TextBox Text="{Binding Arguments, UpdateSourceTrigger=PropertyChanged}" />
                  <TextBlock Text="Working directory" FontWeight="SemiBold" />
                  <TextBox Text="{Binding WorkingDirectory, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Grid.Column="2">
                  <TextBlock Text="Command" FontWeight="SemiBold" />
                  <TextBox Text="{Binding CommandText, UpdateSourceTrigger=PropertyChanged}" Height="82" AcceptsReturn="True" TextWrapping="Wrap" />
                  <TextBlock Text="Shell" FontWeight="SemiBold" />
                  <ComboBox ItemsSource="{Binding ShellKinds}" SelectedItem="{Binding Shell}" />
                  <TextBlock Text="Window mode" FontWeight="SemiBold" />
                  <ComboBox ItemsSource="{Binding WindowModes}" SelectedItem="{Binding WindowMode}" />
                  <TextBlock Text="Environment (KEY=VALUE, one per line)" FontWeight="SemiBold" />
                  <TextBox Text="{Binding EnvironmentText, UpdateSourceTrigger=PropertyChanged}" Height="120" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />
                </StackPanel>
              </Grid>
              <StackPanel Orientation="Horizontal" Margin="-4,8,0,0">
                <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                <Button Content="Launch now" Command="{Binding LaunchCommand}" Style="{StaticResource SecondaryButtonStyle}" />
              </StackPanel>
              <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,8,0,0" TextWrapping="Wrap" />
            </StackPanel>
          </ScrollViewer>
        </Border>
      </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:ProfilesViewModel}">
      <Grid Margin="28">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="285" />
          <ColumnDefinition Width="16" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="10" Padding="14">
          <DockPanel>
            <StackPanel DockPanel.Dock="Top">
              <TextBlock Text="Profiles" FontSize="18" FontWeight="SemiBold" />
              <StackPanel Orientation="Horizontal" Margin="0,8,0,8">
                <Button Content="New" Command="{Binding NewCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource SecondaryButtonStyle}" />
              </StackPanel>
            </StackPanel>
            <ListBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" DisplayMemberPath="Name" BorderThickness="0" />
          </DockPanel>
        </Border>

        <Border Grid.Column="2" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="10" Padding="20">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Profile editor" FontSize="18" FontWeight="SemiBold" />
            <Grid Grid.Row="1" Margin="0,14,0,10">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <StackPanel>
                <TextBlock Text="ID" FontWeight="SemiBold" />
                <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" />
              </StackPanel>
              <StackPanel Grid.Column="2">
                <TextBlock Text="Name" FontWeight="SemiBold" />
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
              </StackPanel>
            </Grid>
            <StackPanel Grid.Row="2">
              <StackPanel Orientation="Horizontal" Margin="-4,0,0,6">
                <Button Content="Add step" Command="{Binding AddStepCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                <Button Content="Remove" Command="{Binding RemoveStepCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                <Button Content="Move up" Command="{Binding MoveStepUpCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                <Button Content="Move down" Command="{Binding MoveStepDownCommand}" Style="{StaticResource SecondaryButtonStyle}" />
              </StackPanel>
              <DataGrid ItemsSource="{Binding Steps}"
                        SelectedItem="{Binding SelectedStep}"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        HeadersVisibility="Column"
                        MinHeight="260">
                <DataGrid.Columns>
                  <DataGridTemplateColumn Header="Tool" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                      <DataTemplate>
                        <ComboBox ItemsSource="{Binding DataContext.AvailableTools, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                  DisplayMemberPath="Name"
                                  SelectedValuePath="Id"
                                  SelectedValue="{Binding ToolId, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0" />
                      </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                  </DataGridTemplateColumn>
                  <DataGridTextColumn Header="Delay before launch (seconds)" Width="220" Binding="{Binding DelayBeforeSeconds, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
              </DataGrid>
            </StackPanel>
            <StackPanel Grid.Row="3">
              <Button Content="Save profile" Command="{Binding SaveCommand}" HorizontalAlignment="Left" Style="{StaticResource PrimaryButtonStyle}" />
              <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,6,0,0" />
            </StackPanel>
          </Grid>
        </Border>
      </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
      <Grid Margin="28">
        <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="10" Padding="22" HorizontalAlignment="Left" Width="520" VerticalAlignment="Top">
          <StackPanel>
            <TextBlock Text="Settings" FontSize="18" FontWeight="SemiBold" />
            <TextBlock Text="Theme" FontWeight="SemiBold" Margin="0,16,0,0" />
            <ComboBox ItemsSource="{Binding Themes}" SelectedItem="{Binding Theme}" />
            <CheckBox Content="Start NGR Launcher with Windows" IsChecked="{Binding StartWithWindows}" Margin="0,10,0,16" />
            <TextBlock Text="Startup registration is persisted here; Windows startup wiring is completed in Phase 4." Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,12" />
            <Button Content="Save settings" Command="{Binding SaveCommand}" HorizontalAlignment="Left" Style="{StaticResource PrimaryButtonStyle}" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,8,0,0" />
          </StackPanel>
        </Border>
      </Grid>
    </DataTemplate>
  </Window.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="220" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Border Background="#172033">
      <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Top" Margin="14,24,14,18">
          <TextBlock Text="NGR" Foreground="White" FontSize="24" FontWeight="Bold" Margin="10,0" />
          <TextBlock Text="Launcher" Foreground="#94A3B8" FontSize="13" Margin="10,0,10,18" />
          <Button Content="Dashboard" Command="{Binding NavigateCommand}" CommandParameter="Dashboard" Style="{StaticResource NavButtonStyle}" />
          <Button Content="Tool Library" Command="{Binding NavigateCommand}" CommandParameter="Tools" Style="{StaticResource NavButtonStyle}" />
          <Button Content="Profiles" Command="{Binding NavigateCommand}" CommandParameter="Profiles" Style="{StaticResource NavButtonStyle}" />
          <Button Content="Settings" Command="{Binding NavigateCommand}" CommandParameter="Settings" Style="{StaticResource NavButtonStyle}" />
        </StackPanel>
        <TextBlock DockPanel.Dock="Bottom" Text="Local · Windows 10/11" Foreground="#64748B" Margin="24,0,0,22" />
      </DockPanel>
    </Border>

    <Grid Grid.Column="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="70" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
              BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}"
              BorderThickness="0,0,0,1">
        <TextBlock Text="{Binding CurrentTitle}" FontSize="23" FontWeight="SemiBold" VerticalAlignment="Center" Margin="28,0" />
      </Border>
      <ContentControl Grid.Row="1" Content="{Binding CurrentPage}" />
    </Grid>
  </Grid>
</Window>
